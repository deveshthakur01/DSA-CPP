#include <algorithm>
#include <iostream>
#include <vector>
#include <limits.h>
//find Max and Min of array 
/* INT_MAX is a macro that specifies that an integer variable cannot store any value beyond this limit. 
INT_MIN specifies that an integer variable cannot store any value below this limit.

Values of INT_MAX and INT_MIN may vary
from compiler to compiler. Following are
typical values in a compiler where integers
are stored using 32 bits.

Value of INT_MAX is +2147483647.
Value of INT_MIN is -2147483648.*/


using namespace std;
int getMini(int num[] ,int size){
	
	int mini= INT_MAX;

	for(int i=0 ; i<size; i++)
	{
		if(num[i]< mini)
		{
			mini=num[i];
		}
	}
	return mini;

}

int getMaxi(int num[],int size){
	int maxi= INT_MIN;

	for(int i=0 ; i<size; i++){
		if(num[i]>maxi)
		{
			maxi=num[i];
		}
	}
	return maxi;

}


int main() {
	// Your code goes here;
	int size;
	cin>> size;

	int arr[100];
for (int i =0;i<size;i++){
	cin>>arr[i];
}
	getMini(arr,size);
	getMaxi(arr,size);

	cout<<"Mini "<<getMini(arr,size)<<endl<<endl;
cout<<"Maxi "<<getMaxi(arr,size)<<endl;

	return 0;
}


/*Custom Input
4
5 8 2 7

Output
[Success] Your code was executed successfully
Mini 2

Maxi 8*/
